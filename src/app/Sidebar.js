import React from "react";
import { Box } from "grommet";

export const Sidebar = (props) => (
  <Box
    width="medium"
    background="light-2"
    elevation="small"
    align="center"
    {...props}
  />
);

