import React from "react";
import { Box } from "grommet";

export const Sidebar = (props) => (
  <Box
    pad ="medium"
    background="light-2"
    elevation="small"
    align="center"
    {...props}
  />
);

